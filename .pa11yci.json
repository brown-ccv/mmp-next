{
  "defaults": {
    "chromeLaunchConfig": {
      "args": [
        "--no-sandbox",
        "--disable-setuid-sandbox",
        "--disable-dev-shm-usage",
        "--disable-gpu",
        "--disable-web-security"
      ]
    }
  },
  "urls": ["http://localhost:3000", "http://localhost:3000/mmp"]
}
